<nav class="navbar navbar-expand-lg fixed-top bg-dark" data-bs-theme="dark">
  <div class="container-fluid">
    <div id="logo" class="ms-md-3 ms-0">
      <%= link_to 'Postula' , root_path, class: 'navbar-brand text-white' %>
    </div>

    <div class="collapse navbar-collapse justify-content-end gap-4 pt-3 pt-md-0 text-white" id="navbarResponsive">
      <% if user_signed_in? %>
        <% if current_user.name.present? %>
          Hola <%= current_user.name %>
            <% else %>
              Hola <%= current_user.email %>

                <% end %>
                  <% else %>

                    <% end %>
                      </a>
                      <ul class="nav navbar-nav navbar-right">
                        <% if user_signed_in? %>
                         <% if current_user.admin? %>
                <li><%= link_to 'Registrar', new_user_registration_path, class: 'dropdown-item px-4'%></li>
                <li><hr class="dropdown-divider"></li>
                         <% end %>
                         <li><%= link_to "Editar perfil", edit_user_registration_path, class: 'dropdown-item' %></li>
                        <hr class="dropdown-divider">
                          <li>
                            <%= button_to 'Cerrar sesión' , destroy_user_session_path,
                              class: 'dropdown-item text-white mx-3' , method: :delete %>
                          </li>
                          <% else %>
                            <li>
                              <%= link_to 'Iniciar sesión' , user_session_path, class: 'dropdown-item text-white px-5'
                                %>
                            </li>

                            <% end %>
                      </ul>
                      </li>
                      </ul>
    </div>
  </div>
</nav>